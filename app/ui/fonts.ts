import { Inter, Lusitana, Rubik_Burned } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: ['400', '700'],
});
export const rubikBurned = Rubik_Burned({ subsets: ['latin'], weight: '400' });
